"""
custom_events.py
Auto-generated by VerseOff.
Use this file to write custom, offline-only Python logic.
This file is NOT overwritten on subsequent builds.
"""
import logging

logger = logging.getLogger(__name__)

def post_load(logical_name: str, form_context: dict):
    """
    Fired after the PyQt form has loaded a record and evaluated Dataverse JS.
    Use this to inject offline-specific UI tweaks or GPS tracking.
    """
    pass

def pre_save(logical_name: str, record_data: dict) -> dict:
    """
    Fired before a record is saved to the local SQLite database.
    Use this to auto-fill fields or perform custom validation.
    Modify and return the record_data dictionary.
    Raise an Exception to block the save.
    """
    return record_data

def pre_sync(logical_name: str, records: list) -> list:
    """
    Fired before pending records are pushed to the online Dataverse server.
    """
    return records

def post_sync(logical_name: str, results: dict):
    """
    Fired after a sync cycle completes.
    """
    pass
